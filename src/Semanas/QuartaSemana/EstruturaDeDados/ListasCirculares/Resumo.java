package QuartaSemana.EstruturaDeDados.ListasCirculares;

public class Resumo {
    /**
     ***********Listas Circulares*************
     * Listas circulares são como as listas encadeadas, porém seu nó de referência CABEÇA (HEAD) aponta pra o nó CAUDA e não para null;
     * Ou seja o nó do último elemento aponta para o elemento do primeiro nó da lista e vice versa;
     * Na lista circular nunca teremos o nó nullo sua ref sempre aponta para outra;
     * Métodos:
     *     .remove();
     *     .get(index);
     *     .add(elemento);
     *     .isEmpty();
     *
     */
}
