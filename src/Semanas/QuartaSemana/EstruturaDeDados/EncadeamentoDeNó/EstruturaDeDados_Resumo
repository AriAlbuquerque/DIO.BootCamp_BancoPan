package QuartaSemana.EstruturaDeDados.EncadeamentoDeNó;

public class EstruturaDeDadosResumo {

    /**
     *********Estrutura de Dados*************
     * Forma de organizar e armazenar dados, da maneira mais viável, que poderão ser utilizados de maneira eficiente;
     * O nó(node) é um espaço em memória que armazena dados ou referência para outro nó;
     * Em JAVA um nó é uma CLASSE;
     * Um node armazena um dado inteiro e uma referência para o próximo node;
     * A última referência de node é null;
     *
     *
     *  *****ESTRUTURA DE PILHAS*********
     *  Estrutura que irá armaxenar dados em forma de pilhas (Como uma pilha de livros);
     *  O último elemento adicionado á pilha será o primeiro a sair, LIFO: (Last in Fist out;
     *  Estrutura de dados em PILHA o NÓ indicará sua referência para o último item adicionado a lista que será a HEAD e assim seguirá;
     *  O primeiro item adicionado a pilha terá indicação para null();
     *
     *
     *    ******** Método TOP *********
     ***Método TOP faz uma cópia ou referência ao elemento no topo da lista, sem exclui-lo ou modifica-lo;
     *EX:    Node node = pilha.top();
     *       int numero = node.getInt();
     *
     * ***********Método POP ***************
     ***Método POP retira o primeiro NÓ da pilha, mudando a HEAD;
     * EX:    Node node = pilha.pop();
     *         int numero = node,getInt();
     *
     *
     *  *************Método PUSH ************
     * ** Método push insere NÓ na lista e indica ele como HEAD;
     * Ex:    Node node = new Node();
     *        node.setInt(1);
     *
     * *******Método isEmpty*******
     * **Método isEmpty retorna se a estrutura de dados não está vazia, se conteúdo não é NULL;
     * Ex:     public boolean isEmpty(){
     *     if (refNode == null){
     *         return true;
     *     }
     *     return false;
     *  }
     *
     *
     *
     */
}
