package QuartaSemana.Generics;

public class MainResumo {
    /**
     * Em generics utilizamos <T> para criar lista genérica ou seja de qualquer coisa, desde que haja um toString;
     *
     * **** WILDCARDS (Coringas)
     * 1-UNKNOW WILDCARD (Unbounded) = Ilimitado, mais genérico;
     * 2-BOUNDED WILDCARD(Upper bounded/Lower Bounded; = Limitado a um método herdeiro;
     *
     * ****CONVENÇÕES:
     * "K" -> para KEY;
     * "V" -> para VALUE;
     * "E" -> para ELEMENT;
     * "T" -> para TYPE;
     * "?" -> para quando GENÉRICO;
     *
     */

}
